{% extends 'base.html.twig' %}

{% block title %}Matiere{% endblock %}

{% block body %}
    <h1>Matiere</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Name</th>
                <td>{{ matiere.name }}</td>
                <td>{{ matiere.classe }}</td>
            </tr>
        </tbody>
    </table>

    <a class="genric-btn info" href="{{ path('matiere_index') }}">back to list</a>
  {% if is_granted('ROLE_SUPER_ADMIN') %}
    <a class="genric-btn primary" href="{{ path('matiere_edit', {'id': matiere.id}) }}">edit</a>

    {{ include('matiere/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
