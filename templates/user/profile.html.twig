{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Your profile</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Email</th>
                <td>{{ app.user.email }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ app.user.roles ? app.user.roles|json_encode : '' }}</td>
            </tr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
 {% for information in app.user.informations %}
        
        <br>
            <div>
                <th>Nom:</th>
                  <td>  {{ information.nom }}</td>
                    
                
                <br>
               
            </div>
          
                    {{ information.prenom}}
            
        </div>
        {% endfor %}
           
        </tbody>
    </table>

   
{% endblock %}
