{% extends 'base.html.twig' %}

{% block title %}Professeur{% endblock %}

{% block body %}
    <h1>Professeur</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Fullname</th>
                <td>{{ professeur.fullname }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ professeur.tel }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ professeur.cin }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ professeur.email }}</td>
            </tr>
        </tbody>
    </table>

    <a class="genric-btn info" href="{{ path('professeur_index') }}">back to list</a>

    <a class="genric-btn primary" href="{{ path('professeur_edit', {'id': professeur.id}) }}">edit</a>

    {{ include('professeur/_delete_form.html.twig') }}
{% endblock %}
